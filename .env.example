PORT=3000
HOST=0.0.0.0
DATA_DIR=./data
# Optional. A random secret is written to $DATA_DIR/secret on first run.
SESSION_SECRET=
# Set to true when serving over HTTPS (needed for a proper installable PWA).
COOKIE_SECURE=false
